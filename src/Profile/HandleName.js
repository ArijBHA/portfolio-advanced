
function HandleName () {
    handleEvent = () => {
      alert(`${item.name}`);
    };
  
    
      return (
        <button onClick={this.handleEvent}>Click on me</button>
      );
    
  }
  
  export default HandleName;
